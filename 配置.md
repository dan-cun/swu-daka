# 打卡工具运行环境配置

> 最后核对：2026-09-08（本机实测）。适用对象：Windows 10/11，项目根目录为 `C:\kaifa\tool\打卡`。

本项目有两条使用路径，环境需求略有不同：

| 路径 | 入口 | 需要 |
| --- | --- | --- |
| **CLI 打卡链路**（核心） | `py -3.11 login_and_checkin.py` | Python 3.10+、pip 依赖、Chrome |
| **Web 端**（可选，薄封装 CLI） | FastAPI 后端 + React 前端 | 在 CLI 基础上加 Node.js 18+、前端依赖 |

---

## 1. 组件清单（实测状态）

| 组件 | 用途 | 最低要求 | 本机实测（2026-09-08） | 状态 |
| --- | --- | --- | --- | --- |
| Windows | 操作系统 | Win10 1809+ / Win11 | Win11 | ✅ |
| Python | 运行 CLI 与 Web 后端 | 3.10+（代码用了 `str \| None` 等新语法），推荐 3.11 | **3.11.9**（`C:\Users\12241\AppData\Local\Programs\Python\Python311\python.exe`，py 启动器默认 3.11） | ✅ |
| pip | 安装 Python 依赖 | 任意新版 | 26.1.2 | ✅ |
| Google Chrome | 统一认证登录（CDP 自动化） | 稳定版 64-bit 任意较新版本 | **152.0.7977.76**（`C:\Program Files\Google\Chrome\Application\chrome.exe`） | ✅ |
| Node.js | 前端 dev/build（仅 Web 端） | 18+（推荐 20/22/24 LTS） | **v24.15.0**（npm 11.12.1） | ✅ |
| npm | 安装前端依赖（仅 Web 端） | 随 Node | 11.12.1 | ✅ |
| git | 版本管理（可选） | 任意 | 2.52.0 | ✅ |

> playwright **只作为 CDP 客户端**连接本机 Chrome，不需要 `playwright install` 下载浏览器。
> Edge 等其他 Chromium 内核浏览器**不保证兼容**，登录链路按 Chrome 测试。

## 2. Python 依赖（pip）

CLI 链路必需 + Web 后端必需，本机实测版本：

| 包 | 版本（实测） | 所属链路 | 说明 |
| --- | --- | --- | --- |
| `requests` | 2.32.5 | CLI + Web | 打卡 API 请求 |
| `playwright` | 1.61.0 | CLI | 驱动 Chrome 完成统一认证登录 |
| `ddddocr` | 1.6.1 | CLI | 登录验证码 OCR（依赖 onnxruntime 1.20.1、pillow 12.3.0、rapidocr-onnxruntime 1.4.4，pip 自动带装） |
| `fastapi` | 0.136.1 | Web 后端 | API 服务 |
| `uvicorn[standard]` | 0.47.0 | Web 后端 | ASGI 服务器 |
| `pydantic` | 2.13.4 | Web 后端 | 数据校验（fastapi 依赖） |

安装命令：

```powershell
py -3.11 -m pip install requests playwright ddddocr fastapi "uvicorn[standard]" pydantic
```

## 3. 端口

| 端口 | 用途 | 说明 |
| --- | --- | --- |
| 9222 | Chrome CDP 调试端口 | 登录期间临时占用，用完自动关闭；被占用时会提示 |
| 8000 | Web 后端（uvicorn） | 只绑定 127.0.0.1，不要改 0.0.0.0 |
| 5173 | Web 前端（Vite dev） | 只绑定 127.0.0.1 |

## 4. 环境变量

| 变量 | 必需 | 说明 |
| --- | --- | --- |
| `SWU_USERNAME` | CLI 登录时 | 校园网账号（也可用 `--username` 参数，但不建议，会留在命令历史里） |
| `SWU_PASSWORD` | CLI 登录时 | 校园网密码（只走环境变量，不要写进任何文件） |
| `CHECKIN_WEB_DATA_DIR` / `CHECKIN_WEB_DATABASE_URL` / `CHECKIN_WEB_LOG_DIR` / `CHECKIN_WEB_CORS_ORIGINS` | 否 | Web 端可选配置，见 `checkin-web/.env.example` |

## 5. 目录与文件

| 路径 | 说明 |
| --- | --- |
| `%TEMP%\ChromeSwuLoginProfile` | 登录用 Chrome 独立 profile，**每次登录前自动清空**，可手动删除 |
| `token.txt`（项目根） | 最新 Token，短命（约一天），过期自动重登，可随时删除 |
| `checkin_cache.json`（项目根） | 当天表单元数据缓存，仅当天有效 |
| `audit_logs/` `network_logs/` `run_logs/` | 运行日志，**含个人信息，禁止外传/提交** |
| `checkin-web/data/app.db` | Web 端 SQLite（用户、凭据、自动任务） |

以上路径均已写入 `.gitignore`。

## 6. 一键安装

### 方式 A：一键脚本（推荐）

项目根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

脚本自动完成：检测/安装 Python 3.11 → pip 依赖（缺失才装）→ 检测/安装 Chrome → 检测/安装 Node.js LTS → 前端 `npm ci`（仅缺 `node_modules` 时）→ 创建数据目录 → 跑 `--env-check` 自检并输出摘要。幂等，可重复执行。

可选参数：

```powershell
.\install.ps1 -SkipWeb                        # 只装 CLI 链路（跳过 Node/前端）
.\install.ps1 -PipMirror "https://mirrors.aliyun.com/pypi/simple/"   # pip 换源
```

### 方式 B：手动命令集

```powershell
# ① 系统组件（缺哪个装哪个；winget 为 Win10 1809+/Win11 自带）
winget install -e --id Python.Python.3.11
winget install -e --id Google.Chrome
winget install -e --id OpenJS.NodeJS.LTS        # 仅 Web 端需要

# ② Python 依赖（在项目根目录）
py -3.11 -m pip install requests playwright ddddocr          # CLI 链路
py -3.11 -m pip install fastapi "uvicorn[standard]" pydantic # Web 后端
# 可选：本机存在冗余 chardet 包，会导致每次导入 requests 打印
# "RequestsDependencyWarning: urllib3 ... doesn't match"（无功能影响），
# 可卸载消除（charset-normalizer 已存在，卸载安全）：
py -3.11 -m pip uninstall -y chardet

# ③ 前端依赖（仅 Web 端，在项目根目录）
cd checkin-web\frontend
npm ci        # 有 package-lock.json 时用 ci；无锁文件时改用 npm install
cd ..\..

# ④ 自检
py -3.11 login_and_checkin.py --env-check
```

## 7. 验证环境是否就绪

```powershell
# CLI 链路自检（输出 Python/Chrome/依赖/端口状态）
py -3.11 login_and_checkin.py --env-check

# Web 后端（checkin-web\backend 目录，系统 Python 直接可跑，无需 venv）
py -3.11 -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# 另开窗口：浏览器访问 http://127.0.0.1:8000/health 应返回 {"status":"ok",...}

# Web 前端（项目根目录）
cd checkin-web\frontend; npm run dev
# 另开窗口：浏览器访问 http://127.0.0.1:5173
```

## 8. 常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 导入 requests 打印 `RequestsDependencyWarning`（urllib3/charset 版本不匹配） | 本机装了冗余的 `chardet 6.0.0.post1`，超出 requests 2.32.5 支持范围。**无功能影响**（API 调用全部正常）；`py -3.11 -m pip uninstall -y chardet` 可消除 |
| `--env-check` 显示 Chrome 未找到 | Chrome 装在非默认路径时用 `--chrome-exe "完整路径"` 指定 |
| 端口 9222 被占用 | 关闭残留 Chrome 或换一个 `--debug-port`；脚本会先清理本工具自己的 profile 进程 |
| 验证码 OCR 失败 | 最多重试 8 次；ddddocr 未装时会自动转"查看 captcha.png 手动输入" |
| ddddocr 安装慢/失败 | 用国内镜像：`py -3.11 -m pip install ddddocr -i https://mirrors.aliyun.com/pypi/simple/` |
| 想隔离依赖（可选） | 按 README 在 `checkin-web\backend` 建 venv 并把 2 节全部依赖装进 venv；当前本机直接用系统 Python 3.11，依赖齐全 |

## 9. 安全红线（摘要）

- 密码只走环境变量/网页输入，**不写代码、不进 git、不进日志**（审计日志已脱敏 token/密码/cookie）。
- Web 后端必须保持绑定 `127.0.0.1`，不配公网转发。
- `token.txt`、`checkin_cache.json`、`audit_logs/`、`network_logs/`、`checkin-web/data/*.db` 含敏感信息，禁止提交或外发。
- Web 自动任务的密码目前是临时明文存 SQLite（`plain-temporary`），启用长期自动任务前先完成加密改造。
